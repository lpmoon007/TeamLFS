# Secrets
.env
.env.local
.env.*.local

# Supabase local
supabase/.branches
supabase/.temp
.branches
.temp

# Node / Next.js (for the Phase 2 UI app)
node_modules/
.next/
out/
dist/
*.log
.DS_Store

# Scratch: Be Legendary design-handoff preview (unpacked zip + headless-render tooling).
# Not part of The Signal; kept out of this repo's history on purpose.
_belegendary_unpack/
_preview/
